<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practical">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practical">
        <activity android:name=".display_form">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login">
        </activity>
        <activity android:name=".selectgender"></activity>
        <activity android:name=".increasecounter" />
        <activity android:name=".secondquestionrelative" />
        <activity android:name=".relative" />
        <activity android:name=".Happybirthday" />
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>